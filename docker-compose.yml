services:
  timeaura-db:
    image: postgres:15-alpine
    container_name: timeaura-postgres
    restart: always
    environment:
      POSTGRES_USER: timeaura_user
      POSTGRES_PASSWORD: TimeAuraTF2k26!
      POSTGRES_DB: timeaura_db
    ports:
      - "5434:5432"
    volumes:
      - timeaura_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U timeaura_user -d timeaura_db"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 15s

  timeaura-app:
    image: timeaura-app:latest
    build:
      context: .
      dockerfile: Dockerfile
      args:
        DATABASE_URL: "postgresql://timeaura_user:TimeAuraTF2k26!@timeaura-db:5432/timeaura_db?schema=public"
        NEXT_SERVER_ACTIONS_ENCRYPTION_KEY: "E1LXKP86V3cT4qgOFRT1NbHebO//9C5NXGhz0HBEMEE="
    container_name: timeaura-app
    restart: always
    ports:
      - "3001:3000"
    environment:
      DATABASE_URL: postgresql://timeaura_user:TimeAuraTF2k26!@timeaura-db:5432/timeaura_db?schema=public
      NEXTAUTH_SECRET: c0/YLmDsVXx+WUyx0IJw+fgH0toH7+NsXU5m5tGlfZc=
      NEXTAUTH_URL: http://timeaura.techforbs.com
      PORT: 3000
      HOSTNAME: 0.0.0.0
      NEXT_SERVER_ACTIONS_ENCRYPTION_KEY: "E1LXKP86V3cT4qgOFRT1NbHebO//9C5NXGhz0HBEMEE="
    depends_on:
      timeaura-db:
        condition: service_healthy

volumes:
  timeaura_pgdata: